# GitHub Requirements

## System Requirements
- Git >= 2.40
- Docker Desktop >= 4.x
- Python >= 3.11

## Git Setup
```bash
git --version
git config --global user.name "Your Name"
git config --global user.email "you@email.com"